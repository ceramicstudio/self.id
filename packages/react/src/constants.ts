import type { CeramicNetwork } from '@self.id/core'
import type { ConnectNetwork } from '@self.id/web'

/** @internal */
export const DEFAULT_CERAMIC_NETWORK: CeramicNetwork = 'testnet-clay'
/** @internal */
export const DEFAULT_CONNECT_NETWORK: ConnectNetwork = 'testnet-clay'

/** @internal */
export const VIEWER_ID_STORAGE_KEY = 'self.id-local-id'
